Run "npm install" to install all the necessary modules.

Folder "./js" cotains the "model" implementation (same as the one in
assignment #1).

Folder "public" contains all static files the client can access.
Folder "public/js" also contain a file name "items.js" but the
function defined in this file is to be called on the client side.
